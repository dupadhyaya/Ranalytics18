# TS - Log of Values

#additive ZA = T + S + I (+ e)
#multiplicative ZM = T * S +* I (+ e)
T=4 ; S=5; I=6
(A = T + S + I)
(M = T * S * I)

#multiplicative to additive
log(M)
log(T)+ log(S) + log(I)

log(32); 5 * log(2); log(2) + log(2) + log(2) + log(2) + log(2) 
